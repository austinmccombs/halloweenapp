<p id="notice"><%= notice %></p>

<h1>Guests</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Items</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guests.each do |guest| %>
      <tr>
        <td><%= guest.name %></td>
        <td><%= guest.items %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Beer Pong Teams', beerpong_path %>
<%= link_to 'New Guest', new_guest_path %>
